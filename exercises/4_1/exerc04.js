// Shows if the number is positive, negative or zero

let verifyNumber = 0;
let result = '';
if(verifyNumber > 0)
    result = 'Positive';
else if(verifyNumber < 0)
    result = 'Negative';
else
    result = 'Zero'

console.log('The number is ' + result)